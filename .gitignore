# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Environment
.env
*.env
.venv
env/
venv/

# Model files (too large for git — stored in ~/models/)
*.gguf
*.bin
*.safetensors

# Editor
.vscode/
.idea/
*.swp
*.swo

# Logs — session inference logs contain full deliberation text; local-only
logs/

# Path B session outputs — full deliberation transcripts + JSON records; local-only
# session_index.jsonl is NOT ignored — it's the git-tracked research history
tracks/path_b/output/results/
codex/impl/path_b/results/

# Memory registers — local session state; local-only
memory/burden_register.txt
memory/burden_register_hashes.txt

# Temporary
*.tmp
*.bak
.claude/
memory/MEMORY.md
